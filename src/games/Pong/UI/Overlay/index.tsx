import AttentionOverlay from "./attention";
import MenuOverlay from "./menu";
import SettingsOverlay from "./settings";

export enum Overlay {
  None,
  Menu,
  Settings,
  Attention,
}

export { AttentionOverlay, MenuOverlay, SettingsOverlay };
